extends KinematicBody2D

var timer = 0.5



# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	# create timer and destroy self after so long 
	# create parent weapon flare and have all children implement from parent flare)
	pass
